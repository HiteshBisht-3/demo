<%if user_signed_in?%>
<div class="flex flex-col items-center justify-center h-screen bg-white">
  <h1 class="text-3xl font-bold mb-2">Welcome to Instagram </h1>
     <p class="text-gray-600 mb-4">continue.</p>
  <%= button_to 'Logout', destroy_user_session_path, method: :delete %>

<%else%>
    <div class="flex flex-col items-center justify-center h-screen bg-white">
   <h1 class="text-3xl font-bold mb-2">Welcome to Instagram </h1>
   <p class="text-gray-600 mb-4">Please register or sign in to continue.</p>
    <%= link_to 'Sign Up', new_user_registration_path, class: 'bg-green-500 text-white px-6 py-2 rounded-full shadow-md hover:bg-green-600' %>
    <%= link_to 'Sign In', new_user_session_path, class: 'bg-blue-500 text-black px-8 py-3 rounded-full shadow-lg hover:bg-blue-600 focus:outline-none foc</div>us:ring-2 focus:ring-blue-400 transition duration-200 transform hover:scale-105' %></div>
<%end%>
